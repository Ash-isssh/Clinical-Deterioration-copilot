# Agentic Clinical Deterioration & Escalation Copilot

## Setup
1. `python -m venv venv && source venv/bin/activate`
2. `pip install -r requirements.txt`
3. `cp .env.example .env` and fill in your API key
4. TODO: dataset download/prep instructions

## Architecture
TODO: paste architecture-diagram.png here and a short walkthrough of the 6 subsystems:
ingestion -> stateful profile -> trend detection -> risk scoring/suppression -> RAG grounding -> agent + human-in-the-loop + audit

## Running
TODO: command to start the simulator + agent + (optional) dashboard

## Demo
See `demo/README.md` for the recorded walkthrough link.
