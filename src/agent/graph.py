"""
LangGraph definition: the shared State object, nodes, and conditional edges
that wire together detection -> RAG grounding -> escalation -> human interrupt -> audit.
"""
# TODO: from langgraph.graph import StateGraph
